{
  "name": "musictokenring",
  "version": "2.0.0",
  "description": "MusicToken Ring Platform - Frontend and Backend",
  "private": true,
  "scripts": {
    "start": "node backend/server-auto.js",
    "dev": "nodemon backend/server-auto.js",
    "preview": "npx --yes serve . -l 8000",
    "check": "node --check backend/server-auto.js && node --check backend/nowpayments-service.js && node --check backend/claim-service.js",
    "check:integrity": "node scripts/verify-runtime-integrity.js",
    "build": "npm run check"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "viem": "^2.0.0",
    "@supabase/supabase-js": "^2.38.0",
    "express-rate-limit": "^7.1.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
